<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@android:color/transparent" /> <!-- 투명한 내부 -->
    <stroke
        android:width="1dp"
        android:color="@color/kakao_brown" /> <!-- 빨간색 테두리, 2dp 두께 -->
    <corners android:radius="4dp" /> <!-- 둥근 모서리, 8dp 반경 -->
    <padding
        android:left="10dp"
        android:top="0dp"
        android:right="10dp"
        android:bottom="0dp" />
</shape>